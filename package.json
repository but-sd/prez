{
  "name": "prez",
  "version": "1.0.0",
  "description": "Lancement du serveur pour charger les slides",
  "main": "index.js",
  "scripts": {
    "start": "marp -s --html  slides",
    "generate:pdf": "marp --pdf --allow-local-files --html --input-dir slides --output build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@marp-team/marp-cli": "^4.2.2"
  }
}
